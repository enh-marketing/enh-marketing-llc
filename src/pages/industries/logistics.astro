---
import Base from "@/layouts/Base.astro";
import * as c from "@/content/industries/logistics";
import { brand } from "@/lib/content";
import { LogisticsPage } from "@/page-bodies/industries-logistics";

/* Head tags are handed to the layout as props; the structured data is emitted
   below as static HTML rather than through the island.

   THE SCHEMA IS BUILT FROM THE DOCUMENT, NOT WRITTEN. `serviceType` joins the
   six service headings the client's own document lists, and `audience` is the
   nine kinds of logistics business its "Who We Work With" section names, so no
   wording here is ours. Like the other three industry pages, the Service is
   not `isPartOf` a pillar: the document sells a combined programme -- "ENH
   Marketing connects search visibility, paid advertising, industry content and
   LinkedIn activity with measurable actions" -- and filing it under one of
   those pillars would contradict that sentence. */

const HREF = "/industries/logistics";

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Logistics Digital Marketing",
  serviceType: c.services.items.map((s) => s.title).join(", "),
  areaServed: { "@type": "Country", name: "United Arab Emirates" },
  audience: c.audience.items.map((a) => ({ "@type": "Audience", audienceType: a.label })),
  provider: {
    "@type": "Organization",
    name: brand.legal,
    url: "https://enhmedia.com",
    telephone: brand.phone,
    address: { "@type": "PostalAddress", streetAddress: brand.address, addressCountry: "AE" },
  },
};
---

<Base title={c.meta.title} description={c.meta.description} canonical={HREF}>
  <script type="application/ld+json" is:inline set:html={JSON.stringify(serviceSchema)} />

  <LogisticsPage client:load />
</Base>
