---
import Base from "@/layouts/Base.astro";
import * as c from "@/content/services/influencer-marketing";
import { brand } from "@/lib/content";
import { InfluencerMarketingPage } from "@/page-bodies/services-social-media-marketing-influencer-marketing";

/* Was `export const metadata` plus a module-scope schema object in
   src/app/services/social-media-marketing/influencer-marketing/page.tsx. The head tags are handed to the layout as props;
   the structured data is emitted below, in the same place in the document it
   occupied before, and as static HTML rather than through the island. */

const HREF = "/services/social-media-marketing/influencer-marketing";

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Influencer Marketing",
  serviceType: "Influencer campaign management, creator selection and compliance",
  areaServed: { "@type": "Country", name: "United Arab Emirates" },
  provider: {
    "@type": "Organization",
    name: brand.legal,
    url: "https://enhmedia.com",
    telephone: brand.phone,
    address: { "@type": "PostalAddress", streetAddress: brand.address, addressCountry: "AE" },
  },
  isPartOf: {
    "@type": "Service",
    name: "Social Media Marketing",
    url: "https://enhmedia.com/services/social-media-marketing",
  },
};
---

<Base
  title={c.meta.title}
  description={c.meta.description}
  canonical={HREF}
>
  <script type="application/ld+json" is:inline set:html={JSON.stringify(serviceSchema)} />

  <InfluencerMarketingPage client:load />
</Base>
